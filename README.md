# ReactNative-GoogleCalendarApi

Application built with React Native, and axios. 
Shows the holidays of any country from Google Calendar public API.

```
1) npm install
2) /ios: pod install
3) run: react-native run-ios
```
