/**
 * @format
 */

import {AppRegistry} from 'react-native';
import CalendarApp from './CalendarApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => CalendarApp);
